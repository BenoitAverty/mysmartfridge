spring:
  jpa:
    show-sql: true # Show SQL generated with Spring data JPA
  profiles:
    active: dev # Activate spring dev profile
server: 
  port: 8080
logging:
  level:
    org.hibernate: INFO
    org.springframework: INFO
    com.baverty: DEBUG